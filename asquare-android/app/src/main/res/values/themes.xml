<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.ASquareStore" parent="Theme.Material3.Dark.NoActionBar">
        <item name="colorPrimary">@color/neon_cyan</item>
        <item name="colorSecondary">@color/neon_purple</item>
        <item name="android:fontFamily">sans</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:statusBarColor">@color/bg_primary</item>
        <item name="android:navigationBarColor">@color/bg_primary</item>
        <item name="android:windowBackground">@color/bg_primary</item>
        <item name="colorSurface">@color/bg_card</item>
        <item name="colorOnSurface">@color/text_primary</item>
        <item name="bottomSheetStyle">@style/ASquareBottomSheet</item>
        <item name="alertDialogTheme">@style/ASquareAlertDialog</item>
    </style>

    <style name="ASquareBottomSheet" parent="Widget.Material3.BottomSheet.Modal">
        <item name="android:background">@drawable/bg_bottom_sheet</item>
        <item name="backgroundTint">@color/bg_secondary</item>
    </style>

    <style name="ASquareAlertDialog" parent="ThemeOverlay.Material3.MaterialAlertDialog">
        <item name="android:colorAccent">@color/neon_cyan</item>
        <item name="android:windowBackground">@drawable/bg_dialog</item>
    </style>
</resources>
